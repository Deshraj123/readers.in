query page(
  $identifier: String
) {

  cmsPage(identifier: $identifier) {
    url_key,
    title,
    content,
    content_heading,
    page_layout,
    meta_title,
    meta_description,
    meta_keywords
  }

}
