overwrite: true
schema: "https://proc.readers.in/graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "fragment-matcher"
  ./graphql/graphql.schema.json:
    plugins:
      - "introspection"
